# setup-tools.ps1
# Deprecated Windows-only wrapper.
#
# Use the WSL/Linux bootstrap instead:
#   bash scripts/setup-tools.sh
#
# This file is kept only so older instructions fail with a clear message
# instead of referencing the deleted winget manifest.

throw 'setup-tools.ps1 is deprecated. Use bash scripts/setup-tools.sh inside WSL/Linux.'
